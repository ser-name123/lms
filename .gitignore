node_modules/
dist/
.next/
out/
build/

# Secrets never go to source control — each host holds its own copy.
.env
.env.*
!.env.example

# DB dumps hold password hashes, session tokens and personal data — never commit.
api/backup/
uploads/

*.log
.DS_Store
.vercel
